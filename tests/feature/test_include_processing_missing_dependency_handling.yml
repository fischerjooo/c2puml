# Test metadata
test:
  name: "Missing Dependency Handling"
  description: "Test handling of missing dependencies"
  category: "feature"
  id: "1012"

---
# Source files
source_files:
  main.c: |
    #include "existing.h"
    int main() { return 0; }
  existing.h: |
    #ifndef EXISTING_H
    #define EXISTING_H
    typedef struct { int val; } Existing;
    #endif

---
# Configuration
config.json: |
  {
    "project_name": "test_missing_dependency_handling",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true,
    "include_depth": 2
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
  model:
    validate_structure: true
    element_counts:
      functions: 1
      structs: 1
  puml:
    syntax_valid: true