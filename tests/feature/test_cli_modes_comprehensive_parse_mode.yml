# Test metadata
test:
  name: "CLI Modes Parse Mode Only"
  description: "Test parse mode only generates model.json through CLI interface"
  category: "feature"
  id: "1006"

---
# Source files
source_files:
  test.c: |
    typedef struct { 
        int x; 
    } Point;
    
    int main() { 
        return 0; 
    }

---
# Configuration
config.json: |
  {
    "project_name": "cli_modes_test",
    "source_folders": ["."],
    "recursive_search": true,
    "output_dir": "./output"
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
    max_execution_time: 30.0
    stdout_contains: "Model saved to"
  
  model:
    validate_structure: true
    project_name: "cli_modes_test"
    expected_files: ["test.c"]
    
    functions_exist: ["main"]
    structs_exist: ["Point"]
    
    element_counts:
      functions: 1
      structs: 1
  
  files:
    output_dir_exists: "./output"
    json_files_valid:
      - "./output/model.json"