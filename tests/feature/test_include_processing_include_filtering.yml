# Test metadata
test:
  name: "Include Filtering"
  description: "Test include filtering functionality"
  category: "feature"
  id: "1009"

---
# Source files
source_files:
  main.c: |
    #include <stdio.h>
    #include "custom.h"
    
    int main() {
        return 0;
    }
  custom.h: |
    #ifndef CUSTOM_H
    #define CUSTOM_H
    
    typedef struct { int val; } Custom;
    
    #endif

---
# Configuration
config.json: |
  {
    "project_name": "test_include_filtering",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true,
    "include_depth": 2
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
  model:
    validate_structure: true
    element_counts:
      functions: 1
      structs: 1
  puml:
    syntax_valid: true