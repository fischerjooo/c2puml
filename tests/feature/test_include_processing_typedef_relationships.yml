# Test metadata
test:
  name: "Typedef Relationships"
  description: "Test typedef relationships across multiple files"
  category: "feature"
  id: "1014"

---
# Source files
source_files:
  main.c: |
    #include "typedef_types.h"
    int main() {
        MyType t;
        return 0;
    }
  typedef_types.h: |
    #ifndef TYPEDEF_TYPES_H
    #define TYPEDEF_TYPES_H
    typedef struct { int val; } MyType;
    #endif

---
# Configuration
config.json: |
  {
    "project_name": "test_typedef_relationships",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true,
    "include_depth": 2
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
  model:
    validate_structure: true
    element_counts:
      functions: 1
      structs: 1
  puml:
    syntax_valid: true