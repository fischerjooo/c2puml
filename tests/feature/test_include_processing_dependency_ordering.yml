# Test metadata
test:
  name: "Dependency Ordering"
  description: "Test that dependency ordering is preserved"
  category: "feature"
  id: "1011"

---
# Source files
source_files:
  main.c: |
    #include "order.h"
    int main() { return 0; }
  order.h: |
    #ifndef ORDER_H
    #define ORDER_H
    typedef struct { int val; } Order;
    #endif

---
# Configuration
config.json: |
  {
    "project_name": "test_dependency_ordering",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true,
    "include_depth": 2
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
  model:
    validate_structure: true
    element_counts:
      functions: 1
      structs: 1
  puml:
    syntax_valid: true