# Test metadata
test:
  name: "Component Features Complex Relationships"
  description: "Test PlantUML generation with complex relationships through the CLI interface"
  category: "feature"
  id: "2005"

---
# Source files
source_files:
  test.c: |
    struct Node {
        int data;
        struct Node* next;
    };

    typedef struct Node Node;

    void process_node(Node* node) {
        // Process node
    }

    int main() {
        Node n;
        process_node(&n);
        return 0;
    }

---
# Configuration
config.json: |
  {
    "project_name": "complex_relationships_test",
    "source_folders": ["."],
    "recursive_search": true,
    "output_dir": "./output"
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
    max_execution_time: 30.0
    stdout_contains: "PlantUML generation complete"
  
  model:
    validate_structure: true
    project_name: "complex_relationships_test"
    expected_files: ["test.c"]
    
    functions_exist: ["process_node", "main"]
    structs_exist: ["Node"]
  
  puml:
    syntax_valid: true
    file_count: 1
    contains_elements: ["Node", "process_node", "main"]
  
  files:
    files_exist:
      - "./output/test.puml"