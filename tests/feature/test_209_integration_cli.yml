test:
  name: Integration CLI Complete Workflow
  description: "End-to-end parse \u2192 transform \u2192 generate with simple project"
  category: feature
  id: '209'
---
source_files:
  main.c: |
    #include <stdio.h>
    #include "config.h"
    #define MAX_SIZE 100nntypedef
         struct Person {
        char name[50];
        int age;
    } Person;
    
    enum Status
         {
        OK,n    ERROR
    };
    
    int global_var = 42;
    
    int main() {
        printf("
        Hello, World!
    ";
        return 0;
    }
    void process_data(void* data) { }n
  config.h: |
    #ifndef CONFIG_H
    #define CONFIG_H
    
    #define CONFIG_VERSION "1.0.0"
        
    
    typedef struct {
        int id;
        char name[100];
    } User;
    
    enum Color
    {
        RED,
        GREEN,
        BLUE
    };
    
    void init_config(void);
    
    #endif
    
assertions:
  execution:
    exit_code: 0
    max_execution_time: 60.0
  model:
    validate_structure: true
    expected_files:
    - main.c
    - config.h
    functions_exist:
    - main
    structs_exist:
    - Person
    - User
    enums_exist:
    - Status
    - Color
    macros_exist:
    - MAX_SIZE
    - CONFIG_VERSION
    includes_exist:
    - stdio.h
    - config.h
  puml:
    syntax_valid: true

---
config.json: |
  {
    "project_name": "auto_test",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true
  }
