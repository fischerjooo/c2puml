# Test metadata
test:
  name: "Include Processing – Error Handling Comprehensive"
  description: "Consolidated missing dependency and generate-only isolation failure checks"
  category: "feature"
  id: "F1101"

---
# Source files
source_files:
  main.c: |
    #include "missing.h"
    int main() { return 0; }

---
# Configuration
config.json: |
  {
    "project_name": "include_processing_errors",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true
  }

---
# CLI execution
cli_execution:
  steps:
    - run_full_pipeline
    - generate_only_isolation_error

---
# Assertions
assertions:
  execution:
    should_succeed: true