# Test metadata
test:
  name: "Function Parameters Parsing"
  description: "Test that function parameters are correctly parsed"
  category: "unit"
  id: "0012"

---
# Source files
source_files:
  function_params.c: |
    int add(int a, int b);
    void process_point(point_t * p);
    real_t average(const int * arr, size_t len);
    int main(int argc, char * argv[]);

---
# Configuration
config.json: |
  {
    "project_name": "test_function_parameters_parsing",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
    output_files: ["model.json", "model_transformed.json", "function_params.puml"]
  
  model:
    files:
      function_params.c:
        functions: ["add", "process_point", "average", "main"]
    element_counts:
      structs: 0
      enums: 0
      functions: 4
      globals: 0
      includes: 0
  
  puml:
    # Global PlantUML assertions (applied to all files)
    syntax_valid: true
    
    # Per-file PlantUML assertions
    files:
      function_params.puml:
        contains_elements: []
        contains_lines: []
        class_count: 1
        relationship_count: 0