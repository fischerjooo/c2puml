# Test metadata
test:
  name: "Function Parameters Display"
  description: "Test that function parameters are correctly displayed in PlantUML"
  category: "unit"
  id: "0013"

---
# Source files
source_files:
  function_display.c: |
    int add(int a, int b) {
        return a + b;
    }
    
    void process_point(point_t * p) {
        // Process point
    }
    
    real_t average(const int * arr, size_t len) {
        return 0.0;
    }

---
# Configuration
config.json: |
  {
    "project_name": "test_function_parameters_display",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
    output_files: ["model.json", "model_transformed.json", "function_display.puml"]
  
  model:
    files:
      function_display.c:
        functions: ["add", "process_point", "average"]
    element_counts:
      structs: 0
      enums: 0
      functions: 3
      globals: 0
      includes: 0
  
  puml:
    # Global PlantUML assertions (applied to all files)
    syntax_valid: true
    
    # Per-file PlantUML assertions
    files:
      function_display.puml:
        contains_elements: ["add", "process_point", "average"]
        contains_lines: ["int add(int a, int b)", "void process_point(point_t * p)", "real_t average(const int * arr, size_t len)"]
        class_count: 1
        relationship_count: 0