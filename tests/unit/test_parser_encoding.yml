# Test metadata
test:
  name: "Parser Encoding"
  description: "Test that files with various encodings can be parsed correctly"
  category: "unit"
  id: "0010"

---
# Source files
source_files:
  encoding.c: |
    // UTF-8 comment with émojis 🚀
    int main() { return 0; }

---
# Configuration
config.json: |
  {
    "project_name": "test_parser_encoding",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
    output_files: ["model.json", "model_transformed.json", "encoding.puml"]
  
  model:
    files:
      encoding.c:
        functions: ["main"]
    element_counts:
      structs: 0
      enums: 0
      functions: 1
      globals: 0
      includes: 0
  
  puml:
    # Global PlantUML assertions (applied to all files)
    syntax_valid: true
    
    # Per-file PlantUML assertions
    files:
      encoding.puml:
        contains_elements: ["main"]
        contains_lines: ["int main()"]
        class_count: 1
        relationship_count: 0