# Test metadata
test:
  name: "Parser Includes"
  description: "Test parsing include statements with different types of includes"
  category: "unit"
  id: "0007"

---
# Source files
source_files:
  includes.c: |
    #include <stdio.h>
    #include <stdlib.h>
    #include "myheader.h"
    #include "config.h"

---
# Configuration
config.json: |
  {
    "project_name": "test_parser_includes",
    "source_folders": ["."],
    "output_dir": "./output",
    "recursive_search": true
  }

---
# Assertions
assertions:
  execution:
    exit_code: 0
    output_files: ["model.json", "model_transformed.json", "includes.puml"]
  
  model:
    files:
      includes.c:
        includes: ["stdio.h", "stdlib.h", "myheader.h", "config.h"]
    element_counts:
      structs: 0
      enums: 0
      functions: 0
      globals: 0
      includes: 4
  
  puml:
    # Global PlantUML assertions (applied to all files)
    syntax_valid: true
    
    # Per-file PlantUML assertions
    files:
      includes.puml:
        contains_elements: []
        contains_lines: []
        class_count: 1
        relationship_count: 0